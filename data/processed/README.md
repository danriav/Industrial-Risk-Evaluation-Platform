# Datasets Procesados

Ubicacion esperada para datasets limpios y normalizados.

El archivo real de calibracion, si se autoriza almacenarlo localmente, debe llamarse:

```text
data/processed/real_training_dataset.csv
```

Si los datos no pueden almacenarse en el repositorio, documentar la referencia segura en `data/real/README.md`.
